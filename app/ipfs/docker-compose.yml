services:
  ipfs:
    image: ipfs/kubo:latest
    container_name: ipfs-public
    # network_mode: "host"
    restart: unless-stopped
    volumes:
      - ./data/ipfs:/data/ipfs
    ports:
      - "5001:5001"   # API for FastAPI
      - "8082:8082"   # Public Gateway
      - "4001:4001"   # Swarm TCP
      - "4001:4001/udp" # Swarm UDP
    environment:
      - IPFS_PROFILE=server
      - IPFS_PATH=/data/ipfs
    entrypoint: >
      sh -c "
        ipfs init --profile server || true &&
        ipfs config Addresses.API /ip4/0.0.0.0/tcp/5001 &&
        ipfs config Addresses.Gateway /ip4/0.0.0.0/tcp/8082 &&
        ipfs config --json Addresses.Swarm '[
          \"/ip4/0.0.0.0/tcp/4001\",
          \"/ip6/::/tcp/4001\",
          \"/ip4/0.0.0.0/udp/4001/quic-v1\",
          \"/ip6/::/udp/4001/quic-v1\"
        ]' &&
        ipfs config --json Addresses.Announce '[
          \"/ip4/18.216.137.19/tcp/4001\",
          \"/ip4/18.216.137.19/udp/4001/quic-v1\"
        ]' &&
        ipfs daemon --migrate --enable-gc --routing=dhtserver
      "



