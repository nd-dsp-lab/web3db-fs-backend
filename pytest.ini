[pytest]
testpaths = tests
# web3 registers a pytest plugin (pytest_ethereum) that fails to import under
# our eth_typing version; we don't use it, so disable it at startup.
addopts = -p no:pytest_ethereum
